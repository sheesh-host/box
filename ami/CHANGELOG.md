# Changelog

## Unreleased

- Initial sheesh.host box AMI: Caddy (static serving + ACME TLS), git-sync
  (content repo), and confd (SSM-driven config). Forked from an internal
  Atlantis AMI and reduced to the sheesh serving stack.
